import { Request, Response } from "express";

export const createChatGroup = (req: Request, res: Response): void => {
  res.json({ message: "curiete cureate" });
};

export const updateChatGroup = (req: Request, res: Response): void => {
  res.json({ message: "upupdatatea" });
};
